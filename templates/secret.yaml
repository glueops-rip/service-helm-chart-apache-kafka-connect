apiVersion: v1
data:kind: Secret
metadata:
  name: regcred-{{ .Values.connect_cluster_name }}
type: kubernetes.io/dockerconfigjson
  .dockerconfigjson: {{ .Values.docker_config_registry_push_credentials }}